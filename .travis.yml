# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

cache: packages
latex: false


branches:
  only:
  - master
  - packagedoc

matrix:
  include:
    - r: 3.3
    - r: release
    - os: osx
      osx_image: xcode7.3
      disable_homebrew: true

addons:
  apt:
    packages:
      - libgit2-dev

